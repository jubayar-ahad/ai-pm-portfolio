# tool-use-agent runtime dependencies.
#
# Only the `ask` slice (live agent loop) needs anthropic — `catalog` and
# `tool` are stdlib-only and run without this install. The dry-run path
# of `ask` is also stdlib-only, so a fresh checkout can exercise the
# prompt and tool schemas without installing anything.
#
# Lower bound matches the rag-app pin so the two builds stay on the same
# SDK floor; upper bound is open so a future SDK refresh does not
# silently break `pip install`.
anthropic>=0.40
